class Amount {
  String id; //Unique id for each expense
  String title;
  String subtitle;
  double? amount;

  Amount({
    required this.id,
    required this.title,
    required this.subtitle,
    required this.amount,
    //
  });
}
